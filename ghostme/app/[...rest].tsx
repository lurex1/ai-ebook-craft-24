/**
 * Trasa catch-all — każdy niedopasowany adres prowadzi do ekranu startowego.
 * Dzięki temu podgląd webowy działa niezależnie od ścieżki URL hostingu,
 * a w aplikacji nie ma "martwych" adresów.
 */

import { SplashScreen } from "@/screens/SplashScreen";

export default SplashScreen;
